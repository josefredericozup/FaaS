'use strict';

/**
 * 
 *
 * @param {Object} cloudEvent A CloudEvent describes event data in common formats to provide interoperability across services, platforms and systems. (See: https://github.com/cloudevents/spec#sdks)
 * 
 */
export default function {{inputs.gcp_cloud_function_name}}(cloudEvent) {
    // Your code here
    // Access the CloudEvent data payload via cloudEvent.data
};